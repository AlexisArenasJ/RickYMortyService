import React from "react";
import { Text } from "react-native";

const Header = () => {
  return (
      <Text style={{textAlign: "center", color: "#fff", fontSize: 20, justifyContent: "center"}}>App Rick And Morty (UPT)</Text>
  );
};

export default Header;
